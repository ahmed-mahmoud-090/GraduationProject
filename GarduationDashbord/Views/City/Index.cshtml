@model List<City>

<a asp-action="Create" class="btn btn-outline-success w-100 mb-2">Create New City</a>
<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-3 mb-4">
                <div class="card custom-card">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title text-center">Name: @item.Name</h5>
                        <div class="mt-auto">
                            <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-outline-success w-100 mb-2">Update</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger w-100">Delete</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .custom-card {
        display: flex;
        flex-direction: column;
        height: 250px; /* ✅ تحديد الطول */
        border-radius: 10px; /* ✅ تحسين شكل الزوايا */
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* ✅ إضافة ظل خفيف */
        transition: transform 0.2s ease-in-out;
    }

        .custom-card:hover {
            transform: scale(1.05); /* ✅ تأثير عند تمرير الماوس */
        }

    .card-body {
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 15px;
    }

    .btn {
        font-size: 14px;
        padding: 8px;
    }
</style>
